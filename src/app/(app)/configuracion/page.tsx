import { SectionPlaceholder } from "@/components/section-placeholder";

export default function ConfiguracionPage() {
  return (
    <SectionPlaceholder
      title="Configuración"
      description="Parámetros por defecto: ratios, merma, factores de barra, márgenes."
      phase="Fase siguiente"
    />
  );
}
